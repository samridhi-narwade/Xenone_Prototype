# Xenone — Python Dependencies (Slack + Discord)

# Slack
slack-bolt>=1.18.0
slack-sdk>=3.26.0

# Discord (keep if running both bots)
discord.py>=2.3.2

# AI/ML — torch installed separately via Dockerfile (CPU-only)
transformers>=4.36.0
accelerate>=0.25.0

# Vector Database
chromadb>=0.4.22

# PDF Generation
fpdf2>=2.7.6

# Utilities
python-dotenv>=1.0.0
